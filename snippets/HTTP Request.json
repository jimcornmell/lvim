{
  "__GET": {
    "body": [
      "###",
      "# @name Some title",
      "# Some description",
      "< {% request.variables.set(\"categories\", [\"UPLOAD\", \"USERTASK\", \"ALLOW_CNP_CARD_REUSE_ONLINE\"]) %}",
      "",
      "GET {{cdlBaseUrl}}/api/v1/parameter/system?category={{categories}}",
      "Authorization: Bearer {{auth_token}}",
      "x-cdl-tenant-id: {{cdlTenantId}}",
      "x-cdl-logId: {{jlpCorrelationId}}",
      "Accept: application/json",
      "",
      "> {%",
      "    import './scripts/validate-201-json-response'",
      "",
      "    client.test(\"Get webRef and policyEventRef\", function () {",
      "        const webRef = jsonPath(response.body, \"\\$.webReference\")",
      "        console.log(`Web reference = \\${webRef}`)",
      "        client.global.set(\"webRef\", webRef)",
      "",
      "        const policyEventRef = jsonPath(response.body, \"\\$.quoteResults[0].policyEventReference\")",
      "        console.log(`Policy event Ref = \\${policyEventRef}`)",
      "        client.global.set(\"policyEventRef\", policyEventRef)",
      "    })",
      "%}",
      "$0"
    ],
    "description": "Get for http",
    "prefix": "__GET"
  },
  "__POST": {
    "body": [
      "###",
      "# @name Some title",
      "# Some description",
      "",
      "@detailLevel=COMPACT",
      "",
      "POST {{cdlBaseUrl}}/api/v1/policies?detailLevel={{detailLevel}}",
      "Authorization: Bearer {{auth_token}}",
      "x-cdl-tenant-id: {{cdlTenantId}}",
      "x-cdl-logId: {{jlpCorrelationId}}",
      "Content-Type: application/json",
      "Accept: application/json",
      "",
      "< data/policyCreate.json",
      "",
      "> {%",
      "    import './scripts/validate-201-json-response'",
      "",
      "    client.test(\"Get webRef and policyEventRef\", function () {",
      "        const webRef = jsonPath(response.body, \"\\$.webReference\")",
      "        console.log(`Web reference = \\${webRef}`)",
      "        client.global.set(\"webRef\", webRef)",
      "",
      "        const policyEventRef = jsonPath(response.body, \"\\$.quoteResults[0].policyEventReference\")",
      "        console.log(`Policy event Ref = \\${policyEventRef}`)",
      "        client.global.set(\"policyEventRef\", policyEventRef)",
      "    })",
      "%}",
      "$0"
    ],
    "description": "POST",
    "prefix": "__POST"
  }
}
}
